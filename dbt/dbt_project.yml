name: podcast_monetization
version: '1.0.0'
config-version: 2

profile: podcast_monetization

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  podcast_monetization:
    staging:
      +materialized: view
    marts:
      +materialized: table
    metrics:
      +materialized: table
